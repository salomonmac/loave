$(document).ready(function(){
    
    
     /*
    $(window).scroll(function{
        if($(this).screenTop()>10)
        {
            $("nav").hide();  //oculta el elemento
        }
        else
        {
             $("nav").hide();  //oculta el elemento
            $(".nav-2").fadeIn();  //Lo aparece con animacion
        }
                            });*/
});